name: Update market data

on:
  workflow_dispatch:
  schedule:
    # Every Monday at 03:00 KST (Sunday 18:00 UTC).
    - cron: "0 18 * * 0"

permissions:
  contents: write

concurrency:
  group: update-market-data
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      EBAY_CLIENT_ID: ${{ secrets.EBAY_CLIENT_ID }}
      EBAY_CLIENT_SECRET: ${{ secrets.EBAY_CLIENT_SECRET }}
      EBAY_MARKETPLACE_ID: EBAY_US
      EBAY_SEARCH_LIMIT: "50"
      EBAY_PSA_SEARCH_LIMIT: "50"
      EBAY_ENGLISH_NM_SEARCH_LIMIT: "50"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Verify scripts
        run: |
          node --check packs.js
          node --check tools/update-ebay-pack-prices.js
          node --check tools/update-ebay-english-nm-prices.js
          node --check tools/update-ebay-japanese-nm-sold-prices.js
          node --check tools/update-box-series-history.js
          node --check tools/audit-price-quality.js
          node --check tools/audit-active-listings.js
          node tools/test-price-outliers.js

      - name: Update eBay pack prices
        if: env.EBAY_CLIENT_ID != '' && env.EBAY_CLIENT_SECRET != ''
        run: node tools/update-ebay-pack-prices.js

      - name: Update eBay English NM prices
        if: env.EBAY_CLIENT_ID != '' && env.EBAY_CLIENT_SECRET != ''
        run: node tools/update-ebay-english-nm-prices.js

      - name: Update eBay Japanese NM sold prices
        if: env.EBAY_CLIENT_ID != ''
        run: node tools/update-ebay-japanese-nm-sold-prices.js --continue-on-error

      - name: Append box price history snapshot
        run: node tools/update-box-series-history.js

      - name: Audit price quality
        run: |
          node tools/audit-price-quality.js --hide-suspicious-nm
          node tools/audit-price-quality.js
          node tools/audit-active-listings.js

      - name: Commit changes
        run: |
          if git diff --quiet data/onepiece-packs.json data/price-quality-audit.json data/japanese-nm-sold-audit.json; then
            echo "No market data changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/onepiece-packs.json data/price-quality-audit.json data/japanese-nm-sold-audit.json
          git commit -m "Update market data"
          git push
